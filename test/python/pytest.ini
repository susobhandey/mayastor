[pytest]
log_cli = true
log_level = warn
console_output_style = classic
asyncio_default_fixture_loop_scope = function
markers =
    requires_kernel_disk: needs a real kernel-attached disk (SMARTHEALTH_DISK_PATH)
    requires_vfio_disk: needs a real VFIO-attached NVMe disk (SMARTHEALTH_VFIO_BDF)
